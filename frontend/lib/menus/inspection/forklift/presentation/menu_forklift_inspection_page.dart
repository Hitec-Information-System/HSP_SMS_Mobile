import 'package:flutter/material.dart';

class MenuForkLiftInspectionPage extends StatelessWidget {
  const MenuForkLiftInspectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Fork Lift"),
      ),
    );
  }
}
