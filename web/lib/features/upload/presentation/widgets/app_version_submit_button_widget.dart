import 'package:flutter/material.dart';

class AppVersionSubmitButton extends StatelessWidget {
  const AppVersionSubmitButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: Text("Submit"),
    );
  }
}
